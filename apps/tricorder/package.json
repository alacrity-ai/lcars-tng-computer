{
  "name": "@tng/tricorder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tricorder cloud API: Hono Worker + per-tenant Durable Object queue + D1 identity, at myhome.computer",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p renderer",
    "build": "tsc --noEmit",
    "build:vs": "vite build -c renderer/vite.config.ts"
  },
  "dependencies": {
    "@tng/contract": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "@fontsource/antonio": "^5.1.0",
    "@tng/panel-renderer": "workspace:*",
    "@tng/shared": "workspace:*",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
