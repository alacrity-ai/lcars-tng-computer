// Phase 3 builds the channel MCP server here: declares the experimental
// claude/channel capability, accepts POST {transcript, confidence} on
// localhost, pushes channel notifications into the running session.
export {};
