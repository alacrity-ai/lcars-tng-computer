{
  "name": "@tng/bridge",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Bridge MCP server: pushes voice commands into the session as channel events; local POST /message + outbound WSS link to the Tricorder cloud queue",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@tng/contract": "workspace:*",
    "ws": "^8.18.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
