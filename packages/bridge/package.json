{
  "name": "@tng/bridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Bridge MCP server: blocking await_message event loop + local message queue (office PTT now, Tricorder cloud link in TNGC-14)",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
