{
  "name": "@tng/panel-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The canonical LCARS panel renderer (TNGC-37): every PanelView component, the frame chrome, the widget overlay, and lcars.css — consumed as source by the wall (apps/web) AND the tricorder viewscreen stage (apps/tricorder/renderer) so a panel is only ever written once.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./lcars.css": "./src/lcars.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "dependencies": {
    "@tng/shared": "workspace:*",
    "astronomy-engine": "^2.1.19",
    "katex": "^0.18.1",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/katex": "^0.16.8",
    "@types/leaflet": "^1.9.21",
    "@types/react": "^18.3.0",
    "react": "^18.3.0",
    "typescript": "^5.6.0"
  }
}
